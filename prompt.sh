#!/bin/bash
# Forgetty development prompt — copy the output of this script as the first message in a new Claude Code session
echo 'Read , then , then , then  Continue with the next uncompleted task from the backlog following the  exactly (Plan → Build → QA ). Do not skip phases.'
