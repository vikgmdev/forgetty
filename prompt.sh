#!/bin/bash
# Forgetty development prompt — copy the output of this script as the first message in a new Claude Code session
echo 'Read CLAUDE.md, then docs/harness/SESSION_STATE.md, then docs/harness/BACKLOG.md, then docs/harness/METHODOLOGY.md. Continue with the next uncompleted task from the backlog following the harness methodology exactly (Plan → Build → QA sequential subagents). Do not skip phases.'
