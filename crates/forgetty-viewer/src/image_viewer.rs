//! Image viewing support.
//!
//! Handles rendering images (PNG, JPEG, SVG, etc.) within the embedded
//! webview panel.

// TODO: Phase 6 — implement image viewer
//
// pub fn render_image(path: &Path) -> Result<String> {
//     // Convert image to a data URI or serve it locally for the webview
// }
